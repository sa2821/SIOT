specVersion: "v1beta"
serviceId: "esp main"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "esp main-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
